"Add: Implemented new template for visualization from search for category;

Update: 'settings.py' of django updated from 'pt-br' to 'en-us';

Update: Models (django) add; Table Categories, rework Table Recips. Add columns: [update, slug, cover];

Update: files officers of django translated from english 'website' > 'django_project', 'plataforma' > 'receps', 'autenticacao' > 'autentication';

Update: system Login, Register and validation, translate for english with the improvement in your style (CSS);

Fixed: adjusted from variables 'recepe' for 'recipe';"

Fixed: Login issue #Is as recursion the function login it's already gone make < but we are importing the function 'login' from django, causing problem problem;

Fixed: Reverse Url #the extension 'recipe_url' redirect to url nonexistent